#!/bin/sh
# Volume over CEC - arch launcher. Execs the build matching the kernel.
D="$(dirname "$0")"
case "$(uname -m)" in
  aarch64|arm64) exec "$D/cecvold.aarch64" "$@" ;;
  *)             exec "$D/cecvold.armv7l"  "$@" ;;
esac
